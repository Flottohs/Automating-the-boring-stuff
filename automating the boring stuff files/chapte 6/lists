





import random


cat = ['at', 'scat', 'fat']

if 'flat' in cat:
    print("reeef true flat is innnnn")



print(random.choice(cat))

#tuple unpacking, add varianbles and there elements into a list

dog = ["fat","hairy","big"]
weight = dog[0]
hair = dog[1]
size = dog[2]


#enumerate cleans up lists if you want to print out position or if the endd of the for is the end of the list
#instead of doing for i in range x to len(fruits ) just put for i, x in enumerate(supplies)


supplies = ['pens', 'staplers', 'flamethrowers', 'binders']
for index, item in enumerate(supplies):
    print('Index ' + str(index) + ' in supplies is: ' + item)



#shuffle
#randomly shuffles lists
#removes unnessisary code to random.randint and all that

random.shuffle(dog)

#finding index of values if you know the value


spam = ['hello', 'hi', 'howdy', 'heyas']
spam.index('hello')
spam.index('heyas')






#insert lets you add values at specific indexes
#>>> bacon = 42
#>>> bacon.insert(1, 'world')


#sort puts all values of list in either size order or alphebetical

dog.sort()
dog.sort(reverse = True)

#reverse values
dog.reverse()

#how to edit strings
name = "sophie a cat"
newname = name[0:7] + "the" + name[8:22]
print(newname)

eggs = ("yay","gay","slay")
#tuples like strngs are ummutable, cannot be changed

#can convert lists and tuples by using
cat = ["rat,","scat","bat"]
print(tuple(cat))
meow = ("reef","kief","chief")
print(list(meow))

#copy and deepcopy
#copy.copy() creates a copy of a list but if you change the copy the original changes
#copy.deepcopy() creates a copy of a list and if you change the copy the original does
import copy
list1 = ['A', 'B', 'C']
list2 = copy.copy(list1)
list3 = copy.deepcopy(list1)
list2[0] = 'X'
list3[1] = 'Y'
print(list1)  # Output: ['A', 'B', 'C']
print(list2)  # Output: ['X', 'B', 'C']
print(list3)  # Output: ['A', 'Y', 'C']